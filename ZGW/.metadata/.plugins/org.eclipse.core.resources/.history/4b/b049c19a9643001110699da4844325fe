#ifndef SOAD_H
#define SOAD_H

#include <stdint.h>
#include "TcpIpH.h"

#define SOAD_MAX_CONNECTIONS 8

typedef enum
{
    SOAD_SOCON_CLOSED = 0,
    SOAD_SOCON_OPEN,
    SOAD_SOCON_CONNECTED
} SoAd_StateType;

typedef struct
{
    uint16_t port;
    uint8_t isTcp;
} SoAd_ConfigType;

void SoAd_Init(const SoAd_ConfigType *cfg);
void SoAd_MainFunction(void);

uint8_t SoAd_OpenSoCon(uint8_t id);
void SoAd_CloseSoCon(uint8_t id);

int32_t SoAd_Send(uint8_t id, const uint8_t *data, uint16_t len);

#endif
