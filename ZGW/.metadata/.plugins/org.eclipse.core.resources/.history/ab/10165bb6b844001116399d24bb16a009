/**
 * @file timer_stm.h
 *
 * NOTE:
 * This file is generated by ACS. Any manual modification done to this file will be lost when the code is
 * regenerated.
 *
 * @cond
 ***********************************************************************************************************************
 * TIMER_STM - Configures a STM timer peripheral to perform timer operations.
 *
 * (c) 2025, Infineon Technologies AG, or an affiliate of Infineon Technologies AG. All rights reserved. 
 *
 * Use of this file is subject to the terms of use agreed between (i) you or the company in which ordinary course of
 * business you are acting and (ii) Infineon Technologies AG or its licensees. If and as long as no such terms of use
 * are agreed, use of this file is subject to following:
 *
 * Boost Software License - Version 1.0 - August 17th, 2003
 *
 * Permission is hereby granted, free of charge, to any person or organization obtaining a copy of the software and
 * accompanying documentation covered by this license (the "Software") to use, reproduce, display, distribute, execute,
 * and transmit the Software, and to prepare derivative works of the Software, and to permit third-parties to whom the
 * Software is furnished to do so, all subject to the following:
 *
 * The copyright notices in the Software and this entire statement, including the above license grant, this restriction
 * and the following disclaimer, must be included in all copies of the Software, in whole or in part, and all
 * derivative works of the Software, unless such copies or derivative works are solely in the form of
 * machine-executable object code generated by a source language processor.
 *
 * THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE
 * WARRANTIES OF MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT SHALL THE
 * COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN
 * CONTRACT, TORT OR OTHERWISE, ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER DEALINGS
 * IN THE SOFTWARE.
 ***********************************************************************************************************************
 *
 * @endcond
 *
 */
/***********************************************************************************************************************
 * HEADER FILES
 **********************************************************************************************************************/
#ifndef TIMER_STM_H
#define TIMER_STM_H

#include "DAVE_Common.h"
#include "IfxStm.h"

/***********************************************************************************************************************
 * MACROS
 **********************************************************************************************************************/

/***********************************************************************************************************************
 * ENUMS
 **********************************************************************************************************************/
/**
 * @ingroup TIMER_STM_enumerations
 * @{
 */
/**
 * @brief enum to describe the possible status values, returned by TIMER_STM APIs.
 */
typedef enum TIMER_STM_STATUS
{
  TIMER_STM_STATUS_SUCCESS = 0U,                /**< Indicates App initialization state successful */
  TIMER_STM_STATUS_FAILURE                      /**< Unknown error */

} TIMER_STM_STATUS_t;

/**
 * @brief compare channels
 */
typedef enum TIMER_STM_COMP_CHANNEL
{
  TIMER_STM_COMP_CHANNEL_0 = 0U,                /**< Compare channel (register) 0 */
  TIMER_STM_COMP_CHANNEL_1,                     /**< Compare channel (register) 1 */
  TIMER_STM_COMP_CHANNEL_MAX                    /**< Compare channel max */

} TIMER_STM_COMP_CHANNEL_t;

/**
 * @}
 */

/***********************************************************************************************************************
* DATA STRUCTURES
***********************************************************************************************************************/
/**
 * @ingroup TIMER_STM_datastructures
 * @{
 */
/**
 * @brief Handler structure of TIMER_STM
 */
typedef struct TIMER_STM_CONF
{
  Ifx_STM                  *stm;                /**< Top address of STM channel */
  uint32                    frequency;          /**< Frequency of the clock input to the STM timer (From CLOCK APP) */

} TIMER_STM_CONF_t;

/**
 * @brief Handler structure of TIMER_STM
 */
typedef struct TIMER_STM
{
  const TIMER_STM_CONF_t   *const app_config;   /**< APP configuration */
  struct
  {
    uint32                  user_interval;      /**< User interval */
    boolean                 auto_start;         /**< Automatic timer start */
    IfxStm_CompareConfig    config;             /**< IfxStm configuration */
    volatile uint32         start_time;         /**< Time on Start() call in microseconds (Calculated by STM counter value) */
    volatile uint32         user_time;          /**< Time elapsed within the user time period (Calculated by STM counter value) */

  } ch[TIMER_STM_COMP_CHANNEL_MAX];             /**< Info for each compare channel */
  boolean                   app_is_initialized; /**< APP initialization status */

} TIMER_STM_t;

/**
 * @}
 */

/***********************************************************************************************************************
 * API Prototypes
 **********************************************************************************************************************/

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @ingroup TIMER_STM_apidoc
 * @{
 */

/**
 * @brief Get the TIMER_STM APP version.
 * @return ACS_APP_VERSION_t APP version information (major, minor and patch number)
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   ACS_APP_VERSION_t app_version;
 *
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   app_version = TIMER_STM_GetAppVersion();
 *   if ((app_version.major == 1) && (app_version.minor == 0) && (app_version.patch == 2)) {
 *     // version of APP met
 *   }
 *
 *   while (1);
 * }
 * @endcode
 */


/**
 * @brief Function to initialize the STM module as per the configuration made in the GUI.
 * @param handle Pointer to static content of APP configuration.
 * @return TIMER_STM_STATUS_t: Status of the STM driver initialization.\n
 *   @ref TIMER_STM_STATUS_SUCCESS - on successful initialization.\n
 *   @ref TIMER_STM_STATUS_FAILURE - if initialization fails.\n
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   TIMER_STM_STATUS_t status = TIMER_STM_Init(&TIMER_STM_0);     // Initialization of APP
 *   if (status != TIMER_STM_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   while (1);
 * }
 * @endcode
 * @note Normally this will be called automatically from DAVE_Init() that are located in core0_main().
 */
TIMER_STM_STATUS_t TIMER_STM_Init(TIMER_STM_t *handle);

/**
 * @brief Function to set time interval
 * @param handle Pointer to static content of APP configuration.
 * @param compare channel which will be used to generate the interrupt
 * @param interval to generate the interrupt as specified by the user
 * @return TIMER_STM_STATUS_t: Status of user timer start.\n
 *   @ref TIMER_STM_STATUS_SUCCESS - on successful start of the user timer.\n
 *   @ref TIMER_STM_STATUS_FAILURE - if start fails.\n
 *
 * \par<b>Description</b><br>
 * This function configures the compare registers of STM based on the settings provided in GUI.
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *
 *   // Set time interval (you can overwrite the value specified in the GUI)
 *   app_status = TIMER_STM_SetTimeInterval(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 1000000);
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *
 *   // Start the timer
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *   while (1);
 * }
 * @endcode
 */
TIMER_STM_STATUS_t TIMER_STM_SetTimeInterval(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel, uint32 user_interval);

/**
 * @brief Function to start the user timer using STM
 * @param handle Pointer to static content of APP configuration.
 * @param compare channel which will be used to generate the interrupt
 * @return TIMER_STM_STATUS_t: Status of user timer start.\n
 *   @ref TIMER_STM_STATUS_SUCCESS - on successful start of the user timer.\n
 *   @ref TIMER_STM_STATUS_FAILURE - if start fails.\n
 *
 * \par<b>Description</b><br>
 * This function configures the compare registers of STM based on the settings provided in GUI.
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *
 *   // Start the timer with the interval specified in GUI
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *   while (1);
 * }
 * @endcode
 */
TIMER_STM_STATUS_t TIMER_STM_Start(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel);

/**
 * @brief Function to update timer compare values to ensure accurate interrupts at the next interval
 * @param handle Pointer to static content of APP configuration.
 * @param compare channel which will be used to generate the interrupt
 * @return TIMER_STM_STATUS_t: Status of user timer start.\n
 *   @ref TIMER_STM_STATUS_SUCCESS - on successful operation of the user timer.\n
 *   @ref TIMER_STM_STATUS_FAILURE - if start fails.\n
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * boolean is_interrupt = FALSE;
 *
 * void STM_interrupt(void)
 * {
 *   TIMER_STM_ClearInterrupt(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);   // Clear the compare match interrupt
 *   TIMER_STM_SetNextInterval(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);  // Set next compare value
 *   is_interrupt = TRUE;
 * }
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 1000000); // Start the timer
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *
 *   while (1)
 *   {
 *     if (is_interrupt)
 *     {
 *       is_interrupt = FALSE;
 *       // Implement your code to do something every timer interval!
 *     }
 *   }
 *   while (1);
 * }
 * @endcode
 */
TIMER_STM_STATUS_t TIMER_STM_SetNextInterval(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel);

/**
 * @brief Function to get the total time since the start of STM timer
 * @param handle Pointer to static content of APP configuration.
 * @return Time elapsed since the start of the STM timer in integer[us]
 *
 * \par<b>Description</b><br>
 * This function gets the total time since the start of the STM timer peripheral.
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   uint32 total_time;
 *   while (1)
 *   {
 *     // Get the total time elapsed since the start of the STM peripheral
 *     total_time = TIMER_STM_GetTotalTime(&TIMER_STM_0);
 *
 *     if ((total_time % 1000000) != 0)
 *     {
 *       // Implement your code to do something every 1 second!
 *     }
 *   }
 * }
 * @endcode
 * @note The maximum time returned from this function is approximately 4294 seconds.
 */
uint32 TIMER_STM_GetTotalTime(TIMER_STM_t *handle);

/**
 * @brief Function to get the time elapsed within the current user interval
 * @param handle Pointer to static content of APP configuration.
 * @return Time elapsed within the current user interval in integer
 *
 * \par<b>Description</b><br>
 * This function gets the time elapsed within the current user interval from the STM counter.
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 1000000); // Start the timer
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *   while (1)
 *   {
 *     // Get the time elapsed within the current user interval
 *     if (TIMER_STM_GetTime(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0) == TIMER_STM_STATUS_SUCCESS)
 *     {
 *       if ((TIMER_STM_0.user_time % 100000) != 0)
 *       {
 *         // Implement your code to do something every 100 ms!
 *       }
 *     }
 *   }
 * }
 * @endcode
 * @note The time information will be stored to handle.user_time in microseconds.
 */
TIMER_STM_STATUS_t TIMER_STM_GetTime(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel);

/**
 * @brief Function to stop the user timer
 * @param handle Pointer to static content of APP configuration.
 * @return void
 *
 * \par<b>Description</b><br>
 * This function stops the user interval by resetting the STM module.
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 1000000); // Start the timer
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *
 *   while (1)
 *   {
 *     // Get the time elapsed within the current user interval
 *     if (TIMER_STM_GetTime(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0) == TIMER_STM_STATUS_SUCCESS)
 *     {
 *       if ((TIMER_STM_0.user_time % 100000) != 0)
 *       {
 *         // Stop the timer when 100 ms is elapsed
 *         TIMER_STM_Stop(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);
 *         break;
 *       }
 *     }
 *   }
 *   while (1);
 * }
 * @endcode
 */
void TIMER_STM_Stop(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel);

/**
 * @brief Returns the running state of the timer.
 *
 * @param handle pointer to the TIMER_STM APP configuration.
 * @return boolean\n true  : if the timer is running\n
 *                   false : if the timer is not running\n
 *
 * \par<b>Description</b><br>
 * TIMER_STM_GetTimerStatus() reads the run bit of the timer to indicate the actual state of the TIMER_STM.
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 1000000); // Start the timer
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *
 *   while (1)
 *   {
 *     // Get the time elapsed within the current user interval
 *     if (TIMER_STM_GetTime(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0) == TIMER_STM_STATUS_SUCCESS)
 *     {
 *       if ((TIMER_STM_0.user_time % 100000) != 0)
 *       {
 *         boolean timer_status = TIMER_STM_GetTimerStatus(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0); // This will return TRUE
 *
 *         // Stop the timer when 100 ms is elapsed
 *         TIMER_STM_Stop(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);
 *
 *         if ((timer_status = TIMER_STM_GetTimerStatus(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0)) == FALSE) // This will return FALSE
 *         {
 *           break;
 *         }
 *       }
 *     }
 *   }
 *   while (1);
 * }
 * @endcode
 */
boolean TIMER_STM_GetTimerStatus(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel);

/**
 * @brief Function to restart the user timer.
 * @param handle Pointer to static content of APP configuration.
 * @param compare channel which will be used to generate the interrupt
 * @param interval to generate the interrupt as specified by the user
 * @return TIMER_STM_STATUS_t: Status of STM restart operation.\n
 *   @ref TIMER_STM_STATUS_SUCCESS - on successful restart.\n
 *   @ref TIMER_STM_STATUS_FAILURE - if restart fails.\n
 *
 * \par<b>Description</b><br>
 * This function restarts the user interval by resetting and starting the STM timer.
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 1000000); // Start the timer
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *
 *   while (1)
 *   {
 *     // Get the time elapsed within the current user interval
 *     if (TIMER_STM_GetTime(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0) == TIMER_STM_STATUS_SUCCESS)
 *     {
 *       if ((TIMER_STM_0.user_time % 100000) != 0)
 *       {
 *         TIMER_STM_Restart(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 500000); // Restart the timer with the new user period value
 *         break;
 *       }
 *     }
 *   }
 *   while (1);
 * }
 * @endcode
 */
TIMER_STM_STATUS_t TIMER_STM_Restart(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel, uint32 user_interval);

/**
 * @brief Function to clear the flag of compare match interrupt
 * @param handle Pointer to static content of APP configuration.
 * @param compare channel which will be used to generate the interrupt
 * @return void
 *
 * \par<b>Example Usage</b><br>
 * @code
 * #include "Ifx_Types.h"
 * #include "IfxCpu.h"
 * #include "IfxScuWdt.h"
 * #include "DAVE.h"
 *
 * IFX_ALIGN(4) IfxCpu_syncEvent g_cpuSyncEvent = 0;
 *
 * boolean is_interrupt = FALSE;
 *
 * void STM_interrupt(void)
 * {
 *   TIMER_STM_ClearInterrupt(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);   // Clear the compare match interrupt
 *   TIMER_STM_SetNextInterval(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0);  // Set next compare value
 *   is_interrupt = TRUE;
 * }
 *
 * void core0_main(void)
 * {
 *   IfxCpu_enableInterrupts();
 *   IfxScuWdt_disableCpuWatchdog(IfxScuWdt_getCpuWatchdogPassword());
 *   IfxScuWdt_disableSafetyWatchdog(IfxScuWdt_getSafetyWatchdogPassword());
 *   IfxCpu_emitEvent(&g_cpuSyncEvent);
 *   IfxCpu_waitEvent(&g_cpuSyncEvent, 1);
 *
 *   DAVE_STATUS_t status = DAVE_Init();           // Initialization of DAVE APPs
 *   if (status != DAVE_STATUS_SUCCESS) {
 *     while (1) {
 *       // Implement your code to handle the error here!
 *     }
 *   }
 *
 *   TIMER_STM_STATUS_t app_status;
 *   app_status = (TIMER_STM_STATUS_t)TIMER_STM_Start(&TIMER_STM_0, TIMER_STM_COMP_CHANNEL_0, 1000000); // Start the timer
 *   if(app_status != TIMER_STM_STATUS_SUCCESS)
 *   {
 *     // Implement your code to handle the error here!
 *   }
 *
 *   while (1)
 *   {
 *     if (is_interrupt)
 *     {
 *       is_interrupt = FALSE;
 *       // Implement your code to do something every timer interval!
 *     }
 *   }
 *   while (1);
 * }
 * @endcode
 */
void TIMER_STM_ClearInterrupt(TIMER_STM_t *handle, TIMER_STM_COMP_CHANNEL_t comp_channel);

/**
 * @}
 */

/* Include App extern declaration file */
#include "timer_stm_conf.h"

#ifdef __cplusplus
}
#endif

#endif /* TIMER_STM_H */

/* CODE_BLOCK_END */
